namespace Libplanet.Node.Options;

internal sealed class ConsensusOptionsValidator
    : OptionsValidatorBase<ConsensusOptions>
{
    protected override void OnValidate(string? name, ConsensusOptions options)
    {
    }
}
